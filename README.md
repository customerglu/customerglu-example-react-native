# CustomerGlu React Native (Expo) Sample

## Getting started

1. Install Expo not already installed

```
npm install --global expo-cli
```

2. Clone the repo and run

```
npm install
```

3. Set the client credentials in App.tsx (line 6 & 7)
4. Run the app

```
npm run start
```
